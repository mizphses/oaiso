<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Oaiso Checkout",
  "short_name": "Oaiso",
  "start_url": "https://oaiso.herokuapp.com/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "serviceworker-rails/#{dim}.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#FFFFFF",
  "background_color": "#FFFFFF",
  "display": "fullscreen",
  "orientation": "portrait"
}
